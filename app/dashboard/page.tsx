import Link from 'next/link'

const data={students:1248,teachers:74,classes:36,attendance:'94.2%',outstanding:'₦18.4m',payments:'₦42.7m'}
export default async function Dashboard({searchParams}:{searchParams:Promise<{role?:string}>}){const {role='owner'}=await searchParams;const roleName=role==='teacher'?'Teacher':role==='parent'?'Parent':role==='student'?'Student':role==='cashier'?'Accountant / Cashier':'Owner / Admin';return <div className="dashboard"><aside className="side"><div className="brand">GB <span style={{color:'#fff'}}>School</span></div><small style={{padding:'0 10px',color:'#8fae9a'}}>DEMO SCHOOL</small>{['Dashboard','Students','Classes','Attendance','Fees & Payments','Results','Reports'].map((x,i)=><Link href={i===0?`/dashboard?role=${role}`:`/${x.toLowerCase().replaceAll(' ','-').replace('&-','')}`} key={x}>{x}</Link>)}<Link href="/login" style={{marginTop:30}}>Switch role</Link></aside><main className="main"><div className="topline"><div><div className="eyebrow">2025/2026 · First Term</div><h1 style={{margin:'5px 0'}}>Good morning, {roleName}</h1></div><span className="pill">LIVE DEMO</span></div><div className="grid grid4"><div className="card"><span className="muted">Students</span><div className="stat">{data.students.toLocaleString()}</div></div><div className="card"><span className="muted">Teachers</span><div className="stat">{data.teachers}</div></div><div className="card"><span className="muted">Attendance</span><div className="stat">{data.attendance}</div></div><div className="card"><span className="muted">Classes</span><div className="stat">{data.classes}</div></div></div><div className="grid grid3" style={{marginTop:18}}><div className="card"><h3>Fee position</h3><p className="muted">Outstanding fees</p><div className="stat">{data.outstanding}</div><Link className="btn secondary" href="/fees-payments" style={{marginTop:16}}>Open fees</Link></div><div className="card"><h3>Payments</h3><p className="muted">Collected this session</p><div className="stat">{data.payments}</div><Link className="btn secondary" href="/fees-payments" style={{marginTop:16}}>View payments</Link></div><div className="card"><h3>Academic results</h3><p className="muted">Latest result processing</p><div className="stat">92%</div><Link className="btn secondary" href="/results" style={{marginTop:16}}>Open results</Link></div></div></main></div>}
