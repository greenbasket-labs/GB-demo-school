import Link from 'next/link'
const reports=[['Student register','1,248 students','Updated today'],['Attendance report','94.2% overall','Updated today'],['Fee collection','₦42.7m collected','Updated today'],['Academic performance','92% processed','First Term']]
export default function Reports(){return <Page><div className="eyebrow">School intelligence</div><h1>Reports</h1><p className="muted">Quick reports for school management.</p><div className="grid grid4" style={{marginTop:22}}>{reports.map(r=><div className="card" key={r[0]}><span className="pill">REPORT</span><h3>{r[0]}</h3><div className="stat" style={{fontSize:21}}>{r[1]}</div><p className="muted">{r[2]}</p><button className="btn secondary">View report</button></div>)}</div></Page>}
function Page({children}:{children:React.ReactNode}){return <div className="dashboard"><aside className="side"><div className="brand">GB <span style={{color:'#fff'}}>School</span></div>{['Dashboard','Students','Classes','Attendance','Fees & Payments','Results','Reports'].map(x=><Link href={x==='Dashboard'?'/dashboard':`/${x.toLowerCase().replaceAll(' ','-').replace('&-','')}`} key={x}>{x}</Link>)}</aside><main className="main">{children}</main></div>}
